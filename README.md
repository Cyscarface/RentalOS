# RentalOS
A scalable property management ecosystem. Engineered with Laravel (REST API) and React (Vite). Includes stateless authentication, comprehensive admin dashboards, role-based access control, and a custom design system.
